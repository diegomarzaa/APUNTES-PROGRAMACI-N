def nombre_funcion(parámetros):
    acciones
    return valor


##################################################################################

cant = int(input("N?"))
for n in range(1, cant):
    if es_primo(n):
        print(n)

        # Pedir otro número
        número = int(input("N?"))

def es_primo(n):                        # CABECERA DE UNA FUNCIÓN
                                    # CUERPO DE UNA FUNCIÓN
    return es_primo


#################################################################################################################################################################################

print(sumatorio(100,200))                                           
def sumatorio(n1, n2):                                              
    return sumatorio()                                              
                                                                    
###################################################################################################################################################################
                                                                    
def repite(cadena, entero):                                         
    return entero * cadena.lower()                                  

print(repite("ABC", 2))


...
suma = suma_columna(M, col)

if suma > máximo:
    máximo = suma

...



def dias_mes(m, a):
    if m ==2:
        if bisiesto(a):
            return 29
        else:
            return 28


def mayor(a, b):
    if a>b:
        return a
    else:
        return b





# UNA FUNCION QUE NO DEVUELVE NADA SE LLAMA PROCEDIMIENTO, MOSTRAR, IMPRIMIR, ETC SON PROCEDIMIENTOS QUE NO DEVUELVEN NADA Y SOLO HACEN ALGO CON LOS DATOS QUE RECIBEN COMO PARAMETROS DE ENTRADA
# UNA FUNCION PUEDE DEVOLVER VARIOS VALORES MEDIANTE UNA LISTA

# UNA TUPLA ES UNA LISTA INMUTABLE (NO SE PUEDE MODIFICAR) Y SE PUEDE ACCEDER A SUS ELEMENTOS MEDIANTE INDICES (COMO LAS LISTAS) 
[a, b, c] = [1, 2, 3]
a, b, c = 1, 2, 3
# UNA TUPLA SE PUEDE CREAR CON PARENTESIS O SIN PARENTESIS
